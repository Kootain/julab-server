module.exports = function local(){
	/*
	 *	get all devices connected to the router
	 *  return:Array of {ip:'',others...}
	 */
	local.prototype.getDevices(){
		//do something
		return [{ip:'1,1,1,1'}];
	}
	
}